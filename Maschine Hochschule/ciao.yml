-  hosts:
    - googlemaster
    - googleknoten
   remote_user: labadmin
   connection: ssh
   gather_facts: true
   become: yes  
   tasks:
     - name: herunterfahren
       community.general.shutdown:


#-  hosts: master
#   remote_user: labadmin
#   connection: ssh
#   gather_facts: true
#   become: yes  
#   tasks:
#     - name: herunterfahren
#       community.general.shutdown:
